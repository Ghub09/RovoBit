{
  "name": "bitex-trading-web",
  "version": "1.0.0",
  "main": "index.js",
 "scripts": {
  "client": "yarn --cwd client install && yarn --cwd client build",
  "server": "yarn --cwd server install",
  "build": "yarn client && yarn server",
  "start": "node server/index.js",
  "dev": "concurrently \"yarn --cwd client dev\" \"yarn --cwd server dev\""
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bitex-trading-web": "file:"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
